import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles (theme => ({
  root: {
    backgroundColor: '#f38bd1',
    '&:hover': {
      backgroundColor: '#D741A6'
    }
  }
}))

export default useStyles;
